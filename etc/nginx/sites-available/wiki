server {
  listen              80;       # Listen on port 80 for IPv4 requests
  server_name         wiki.visual-salade.com;
  root                /home/visual-salade.com/;

  location / {
    proxy_pass        http://127.0.0.1:4567;
    proxy_redirect    off;
  }
}
